/* keeping possible filter values as an object in order to reuse them
in different places of the app.
*/
export const statusFilters = Object.freeze({
  all: 'all',
  active: 'active',
  completed: 'completed',
});
